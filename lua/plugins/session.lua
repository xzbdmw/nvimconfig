return {
    -- "Shatur/neovim-session-manager",
    -- config = function()
    --     local Path = require("plenary.path")
    --     local config = require("session_manager.config")
    --     require("session_manager").setup({
    --         pre_save_cmds = function()
    --             return [[set winwidth=50]]
    --         end,
    --     })
    -- end,
}

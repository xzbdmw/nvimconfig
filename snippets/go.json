{
  "ifl": {
    "body": [
      "if ${1:}, ok := ${2:}; ${3:ok} {",
      "\t${0:}",
      "}"
    ],
    "prefix": "ifl"
  },
  "open": {
    "body": [
      "${1:f}, err := os.OpenFile(logFile, os.O_APPEND|os.O_CREATE|os.O_WRONLY, 0777)",
      "if err != nil {",
      "\t${0:}",
      "\treturn",
      "}"
    ],
    "prefix": "open"
  },
  "sp": {
    "body": "fmt.Sprintf(\"${0:}\")",
    "prefix": "sp"
  }
}
